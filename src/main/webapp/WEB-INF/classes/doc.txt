git init
git status
git add .
git status
git commit -am 'first commit'
git status
git remote add origin git@github.com:ancunho/vip.git
git branch
git push -u origin master

git pull(첨에 할때)

git push -u -f origin master(강제推送)

创建分支
git checkout -b v1.0 origin/master
git push origin HEAD -u